services:
  - type: web
    name: sivasprojects
    runtime: static
    buildCommand: echo "static site"
    staticPublishPath: ./
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
