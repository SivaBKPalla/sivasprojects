# Siva's Projects

A small homepage of my work. The OSCE Station Simulator lives in its own repo and is only linked from here.

**Live OSCE app:** https://osce-simulator-web.onrender.com

## Deploy on Render

1. Create a GitHub repo from this folder.
2. On Render, add a **Static Site** (or use the Blueprint with `render.yaml`).
3. Publish directory: `.`

This site does not contain the OSCE program.
